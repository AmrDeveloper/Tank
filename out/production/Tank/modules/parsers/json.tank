native func parsers:json.jsonObject(source);

native func parsers:json.jsonArray(source);

native func parsers:json.jsonObjectValue(jsonObject, key);

native func parsers:json.jsonArrayObject(jsonArray, index);

native func parsers:json.jsonObjectArray(jsonArray, key);

native func parsers:json.jsonArrayLength(jsonArray);